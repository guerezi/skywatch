name: skywatch
description: A unique blend of social media and real-time weather updates.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.2
  flutter_clean_architecture: ^5.0.4
  geolocator: ^10.1.0
  home_widget: ^0.3.0
  intl: ^0.18.0
  shared_preferences: ^2.1.2
  mobx: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
